import { GetPlayerPitsPipe } from './get-player-pits.pipe';

describe('GetPlayerPitsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetPlayerPitsPipe();
    expect(pipe).toBeTruthy();
  });
});
