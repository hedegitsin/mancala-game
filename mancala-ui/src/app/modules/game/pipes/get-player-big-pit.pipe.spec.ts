import { GetPlayerBigPitPipe } from './get-player-big-pit.pipe';

describe('GetPlayerBigPitPipe', () => {
  it('create an instance', () => {
    const pipe = new GetPlayerBigPitPipe();
    expect(pipe).toBeTruthy();
  });
});
